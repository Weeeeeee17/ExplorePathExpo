# v0.9.1｜蛋故事定稿與分頁順序測試報告

日期：2026-08-31。Windows／Node24.19.0／npm11.17.0。沿用v0.9.0，僅整合使用者提供的蛋故事、確認的名稱與底部分頁順序；仍是開發測試版。

## 更新範圍

- 從左至右：探索 → 夥伴 → 好友 → 足跡 → 健康。保留原頁面ID、圖示、預設探索頁與功能。
- 12篇引言與神秘背景原文定稿；木系「枯木之種」，星系「觀星者的凝望」。對應wood／stargazer ID與原圖不改。
- 個人故事頁與好友故事共用內容及定稿／草稿狀態。幼年名稱、角色目標、8篇幼年章仍待審。
- 世界觀提示明確區分文案與功能：沒有新增心跳／濕度感測、步頻同步動畫、磁性導航或回憶星芒。不改XP／素材孵化條件。
- 既有暱稱不自動改名；收藏、經驗、獎勵帳本與備份格式不變。沒有新SQL、權限、套件版本或付費服務。

## 已完成的本機驗證

- npm run typecheck：通過。
- npm run validate：通過，檢查v0.9.1版本與原SDK54／健康／好友基線。
- npm test：101／101通過，保留原96項，新增5項故事／更名／分頁回歸。
- 12組名稱、引言與背景逐字比對 DevelopmentSupport/approved-egg-stories-v091.json；定稿蛋與8篇待審幼年章解鎖、好友與個人文字一致。
- wood舊資料正規化後ID、原暱稱、XP、共同紀錄及帳本保留；閱讀故事不修改收藏。
- 分頁陣列的ID與文字順序測試通過；畫面仍以ID切換，不以新位置索引決定功能。
- npx expo export --platform ios --output-dir dist-v091-ios --clear --max-workers 2：成功，1226模組、20張圖、約4.02MB Hermes。
- 初次檢查發現測試中舊版本斷言、新測試Node URL型別與JSON鍵序比較問題；已修正測試程式並重跑通過。未為通過測試而改動養成規則。

## ZIP交付驗證

- 候選ZIP共116項目，使用原封裝器及全新目錄，不覆蓋v0.9.0或既有解壓資料。
- 解壓後以Windows PowerShell5執行新版Start-ExplorePath.ps1 -VerifyOnly；npm ci安裝810套件成功。第二次檢查不重裝，型別及101項測試再次通過。
- npm提示esbuild安裝腳本未個別批准，但實際tsx／測試可運行；沒有批量批准未知腳本，也沒有使用--force或--legacy-peer-deps。
- 本版iOS匯出已驗證；未另啟動手機連線測試伺服器，VerifyOnly並非真機啟動證據。
- 最終ZIP交付前逐項比對候選的非Markdown內容及20張原圖SHA256；只允許更新本報告／筆記等Markdown文件。打包器排除node_modules、dist、.git、.expo、實際.env、.npmrc與log。

## 尚待人工／真機驗收

- iPhone新版QR啟動後，五個分頁順序、各分頁內容及選取狀態。
- 長引言／背景在小螢幕與放大字體下完整顯示、可捲動；好友Alert長文與換行。
- 舊版收藏跨版本開啟、既有暱稱／XP／紀錄仍在；兩支同版手機顯示相同故事。
- v0.9.0列出的GPS／計步／本機通知／真實Supabase雙手機驗收仍待完成，不因本版自動測試而宣稱通過。

## 保留限制

SDK54、RN0.81.5、React19.1.0沿用，20張原圖與Metro大JPG尺寸讀取相容處理不改。僅封閉測試；既有工具鏈弱點、前景位置限制、無完整收藏雲端同步等仍見 v0.9.0-test-report.md。

首次啟動請先備份、停止舊伺服器、解壓縮到新資料夾並雙擊最外層Start-ExplorePath.bat。不要刪除Expo Go，不把舊版測試結果當成本版真機證據。

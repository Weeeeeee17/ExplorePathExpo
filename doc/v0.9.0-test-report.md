# v0.9.0｜新系列寵物養成測試報告

日期：2026-08-31。Windows、Node24.19.0、npm11.17.0。依已核准R1–R12及使用者開發／ZIP授權實作。這是封閉開發測試版，不是已上線產品。

## 已驗證

- npm run typecheck：通過，沒有停用型別檢查。
- npm run validate：通過；保留SDK54、個人探索／健康／好友／隱私基線檢查，更新本版寵物及備份v4檢查。
- npm test：96/96通過。原62項基線保留，新增34項（含寵物、原圖打包、展示RPC）。
- npx expo install --check：Dependencies are up to date。未改動依賴版本，package／app／lock根版本同步0.9.0。
- npx expo export --platform ios --output-dir dist-v090-ios --clear --max-workers 2：1226模組、Hermes約4.02MB、20/20寵物圖，匯出成功。
- 原圖12蛋＋8幼年合計55,908,031 bytes；與使用者Desktop原稿逐一SHA256相同。未重畫、壓縮、裁切、去背或剝除附加資料；清單見pet-asset-integrity.json。
- 5圖原始JPG尺寸標頭在預設512KiB讀取範圍之外。重現打包失敗後，新增限定assets/pets JPG的完整讀取，上限8MiB，主程序與worker均套用；直接Metro資產資料測試及整體匯出成功。未修改node_modules檔案。
- 新第三份SQL在PGlite執行，驗證只能修改本人外觀、拒絕非法系列、未公開完整本機資料。未執行任何遠端migration。

## 規則回歸範圍

首蛋0XP、100–150XP邊界、本人步數、出發綁定、ID重複結算／重開、舊紀錄不補發；12系列不重複袋；3000門檻之後才計10趟、低XP暫停、獨立重複個體；缺圖不跳階、不封頂、不降階、補圖自動進化；蛋／非同行凍結、24h衰減及獨立照顧冷卻、心情／清潔上限、切換確認／撤回／鎖定；72h與7天邊界、道具24h、回憶重逢、5趟道具與溢出、全部回憶補蛋；舊資料封存、備份v4往返、公開最小資料、雲端房間消失解除孤立旅程鎖；既有健康、地點、四季與好友權限回歸。

大部分為純規則與結構／資料庫測試，不等同操作iPhone畫面。PGlite不等同Supabase Auth、Realtime或Cron。

## ZIP驗證

- 同一封裝器的候選ZIP共113個項目；全新解壓後用Windows PowerShell5執行最外層指向的啟動器 -VerifyOnly，npm ci安裝810套件成功，第二次不再安裝，結構檢查通過。
- 全新副本再跑型別及96項測試均通過。未使用--force或--legacy-peer-deps；npm提示esbuild安裝腳本尚待個別批准，但實際tsx／測試均可運行，沒有批量批准未知腳本。
- 實際SDK54 Metro測試埠8099回傳runtime exposdk:54.0.0、App0.9.0；iOS開發bundle1337模組、HTTP200。水滴之晶原圖請求HTTP200，6,460,222 bytes完整傳回，測試後停止伺服器。
- 打包器核對20張圖及package-lock版本；排除node_modules、dist、.expo、.git、實際.env、.npmrc與log。不內含任何金鑰或復原密語。
- 最終ZIP會以相同封裝器產生，交付前再比對候選的非Markdown內容；只允許更新驗證報告／筆記等Markdown文件。

## 尚待真機／雙手機驗收

- [ ] iPhone App Store Expo Go掃描新版QR、五分頁顯示、中文字體／小螢幕／原圖載入與記憶體表現。
- [ ] 首次一般探索取得蛋、後續獎勵、重開App保存、GPS／計步／離線恢復。
- [ ] 同行切換／一次撤回、陪伴清潔、背景跨天回到前景、缺圖等待提示。
- [ ] 72h倒數、道具尋回、共同回憶與附近新地點、永久回憶與重逢信物。
- [ ] iOS通知允許／拒絕／關閉、排程時間及實際送達；程式排程不保證系統送達。
- [ ] 真實與展示資料來回不互相污染；舊備份還原前先匯出原資料，預覽確認及照片提示。
- [ ] 使用者Supabase免費專案套用新增第三份migration，兩支同版手機驗證自身／好友外觀、故事與打卡一致。
- [ ] 組隊完成、退出／被移除／轉個人、背景恢復與離線重試，不重發寵物獎勵。
- [ ] 原v0.8好友／位置／任務完整真機驗收清單。

## 待審內容與限制

12系列蛋名與8組素材配對已核准；本次幼年名字、角色目標、12序章與8幼年章仍是草稿，App明確標示，全文見pet-story-drafts.md。尚缺4組幼年及全部成長／成熟圖，不使用過往總覽圖補位。

未新增HealthKit、全天步數、持續背景定位、伺服器驗證XP或跨手機收藏同步；原前景30秒位置互享限制不變。四季旅程維持現行紀錄／信物流程，不額外發一般寵物XP或季節照顧獎勵。

嚴格不啟用付費服務，免費額度不足接受暫停；無全民規模零成本保證。SDK54工具鏈已知弱點沿用v0.8.1揭露，這次JPEG讀取修正不是安全修補。只在可信任網路與少數真人間測試，不公開Metro、不處理未知建置素材，不宣稱audit安全認證。

相容來源：[Expo官方SDK不相容說明](https://docs.expo.dev/troubleshooting/expo-go-version-mismatch/)，2026-08-31核對：iPhone App Store Expo Go支援SDK54。這是選擇相容基線的依據，不是本版真機通過的證據。
